# ENV Var Confessional

Helps identify missing, incorrect, or conflicting environment variables across different environments. Run: `node env-confessor.js` and watch your ENV variables confess their sins.